version: "3.8"
services:
  warp-proxy:
    build: .
    container_name: warp-proxy
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - "40000:40000"
    environment:
      - PROXY_PORT=40000
    restart: unless-stopped
